声明指针变量是必须制定指针所指向变量的类型，因为不同的变量类型占用不同的存储空间
指针声明：
int * pi	// pi是指向int类型白娘的指针
char * pc
float * pf, * pg
类型说明符表明了指针所指向对象的类型，星号*表明声明的变量是一个指针
int * pi
声明的意思是pi是一个指针， *pi是int类型
不要把指针认为是整数类型
指针是一个新类型，不是整数类型。因此ANSI C专门为指针提供了%p格式的转换说明
