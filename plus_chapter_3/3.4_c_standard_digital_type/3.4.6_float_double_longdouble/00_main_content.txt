1、C标准规定，float类型必须至少能表示6位有效数字。
2、float类型必须至少精确表示小数后的6位有效数字。
3、通常，系统储存一个浮点数要占用32位。
其中8位用于表示指数的值和符号，剩下24位用于标识非指数部分及其符号
4、double类型和float类型的最小取值范围相同，但至少必须能表示10位有效数字
5、一般情况下double占用64位而不是32位。一些系统将多出的32位全部用于表示非指数
6、long double类型用以满足比double类型更高的精度要求。不过c只保证long double类型至少与double类型的精度相同
