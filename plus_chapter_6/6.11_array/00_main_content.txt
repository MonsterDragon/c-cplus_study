数组是按顺序储存的一系列类型相同的值，如10个char类型的字符或15个int类型的值。整个数组有一个数组名，通过整数下标访问数组中单独的项或元素float debts[20]
数组元素的编号从0开始
debts[5] = 32.54;
debts[6] = 1.2e+21;
scanf("%f", debts[4]);
注意C编译器不会检查数组下标是否正确
如果char类型的数组末尾包含一个表示字符串末尾的空字符\0，则该数组中的内容就构成了一个字符串。即使字符串也是字符数组
用于标识数组元素的数字被称为下标subscript、索引indice、偏移量offset
